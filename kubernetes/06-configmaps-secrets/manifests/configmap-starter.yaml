apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-app-config
data:
  # TODO: add a MESSAGE key with any greeting text you like
  MESSAGE: "???"
  # TODO: add a GREETING_LANG key, e.g. "en"
  GREETING_LANG: "???"
