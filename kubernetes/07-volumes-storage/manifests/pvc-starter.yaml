apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hello-app-data
spec:
  accessModes:
    # TODO: this claim will be mounted read-write by a single Pod at a time — which access mode is that?
    - ???
  resources:
    requests:
      # TODO: how much storage to request, e.g. "1Gi"
      storage: ???
