apiVersion: v1
kind: Pod
metadata:
  name: hello-app-emptydir
  labels:
    app: hello-app-emptydir
spec:
  containers:
    - name: hello-app
      image: hello-app:v1
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 5000
      volumeMounts:
        - name: data
          # TODO: mount path — must match where app.py writes count.txt (DATA_DIR, default /data)
          mountPath: ???
  volumes:
    - name: data
      # TODO: declare this volume as an emptyDir (a bare, ephemeral, per-Pod scratch dir)
      ???: {}
