apiVersion: v1
kind: Service
metadata:
  name: hello-app
spec:
  type: ClusterIP
  selector:
    # TODO: which label do the hello-app Pods carry? (see Module 4's Deployment)
    app: ???
  ports:
    - port: ???        # TODO: the port other things in the cluster will use to reach this Service
      targetPort: ???  # TODO: the port the container actually listens on (see app.py)
