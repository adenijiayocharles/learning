# TODO: choose a small official Python base image (e.g. python:3.12-slim)
FROM ???

# TODO: set the working directory inside the container
WORKDIR ???

# TODO: copy requirements.txt first (before the app code) so Docker can cache
# the pip install layer separately from your app code — editing app.py later
# shouldn't force a full reinstall of dependencies.
COPY ??? ???

# TODO: install the dependencies listed in requirements.txt
RUN ???

# TODO: now copy the rest of the application code
COPY ??? ???

# TODO: document which port the app listens on (see app.py)
EXPOSE ???

# TODO: the command to run when the container starts
CMD ???
